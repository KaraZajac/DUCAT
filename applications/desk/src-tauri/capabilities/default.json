{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "What the main window may ask of the desk: its own commands, native file and folder pickers, and opening a folder in the OS file manager.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "notification:default"
  ]
}
