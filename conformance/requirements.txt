# What the checkers need beyond the standard library.
#
# The second implementation deliberately re-derives everything it can, but
# three primitives are not worth re-deriving in Python: a JSON-Schema
# validator, Ed25519/P-256 signature verification, and Argon2id. Pinned
# loosely — the point of a second implementation is that it is an
# independent reading of the spec, not an independent cryptography library.
#
#   python3 -m pip install -r conformance/requirements.txt
jsonschema>=4.0
cryptography>=41.0
argon2-cffi>=21.0
